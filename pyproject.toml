[project]
name = "qgis-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cargo>=0.3",
    "fastmcp>=2.2.6",
    "mcp[cli]>=1.3.0",
]
